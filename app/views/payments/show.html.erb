<%# <%= render "layouts/navbar" %>
<div class="m-4 card-border">
  <div class="container info-card card-dem">
    <div class="row">
      <span class="info-top col">
        <h1><%= @payment.payment_title %></h1>
        <h3>Date : <%= @payment.payment_date %></h3>
        <h3>Montant : <%= @payment.payment_amount.to_i %> MAD</h3>
        <h3>Statut : <%= @payment.payment_status %></h3>
        <div class="info-bottom col-8 search-btns">
        </div>
        <h2>Justificatifs</h2>
        <% @payment.photos.each do |photo| %>
          <%= cl_image_tag photo.key, alt: "Pièce justificative de #{@payment.payment_title}"%>
        <% end %>
      </span>
    </div>
  </div>

      <%= link_to "Update", edit_payment_path(@payment.id), class: "" %>
      <%= link_to 'Back', root_path %>
    </div>
</div>
</div>
</div>

<%# <%= render "layouts/footer" %>





</div>
